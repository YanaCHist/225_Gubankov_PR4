<Window x:Class="PractikWork4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PractikWork4"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1115" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="FirstPage" Content="Задание 1" Click="FirstPage_Click" Style="{StaticResource ButtonAlert}"/>
            <Button Name="SecondPage" Content="Задание 2" Click="SecondPage_Click" Style="{StaticResource ButtonAlert}"/>
            <Button Name="ThirdPage" Content="Задание 3" Click="ThirdPage_Click" Style="{StaticResource ButtonAlert}"/>
        </StackPanel>
        <Frame Grid.Row="1" Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
